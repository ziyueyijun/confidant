# 09: 表格与任务列表交互

**What to build:** 结构化块的使用手感(规格 9.6):光标在表格内 → 右键菜单增删行/列、对齐;Tab = 移到下一格,表尾 Enter = 加行;任务列表:点击复选框切换完成态(写盘为 `- [x]`/`- [ ]`),列表缩进 Tab、反缩进 Shift+Tab(右键菜单同步提供)。

- 所有结构变更(增删行列、勾选、缩进)纳入 08 的撤销/重做链路;序列化往返正确(表格结构、任务完成态、嵌套缩进保存→重开不丢)。
- 登记首验项(附录 B-2):表格单元格编辑手感、长文滚动/渲染性能——自动化覆盖不到,20 人工执行,本票先把可用实现做扎实(单元格可自由编辑、长表格滚动不卡)。
- 嵌套任务列表/列表转任务列表等边界:由菜单(08)建结构,本票交互不改源语义。

**Blocked by:** 08(块级段落命令与撤销重做——插入表格与任务列表的入口)。

**Status:** ready-for-agent

- [x] 光标在表格内右键:增删行、增删列、单元格对齐可用;结构变更后表格仍是合法 Markdown,往返不丢。
- [x] Tab 移下一格(末格行为合理,如表尾自动加行由 Enter 承担);表尾 Enter 加新行。
- [x] 单元格内可自由编辑文本(含 IME 输入不受阻)。
- [x] 任务列表:点击复选框切换完成/未完成,写盘为 `- [x]`/`- [ ]`,往返正确。
- [x] 任务项 Tab 缩进、Shift+Tab 反缩进;右键菜单同步提供;嵌套层级往返不丢。
- [x] 上述全部结构变更可撤销/重做(08 链路)。

## 实现记录(09)

- 提交:feat(09)(分支 impl/confidant-v1)。

- 表格:右键(光标定位到格 → native 菜单)在上方/下方插入行、左侧/右侧插入列、删除本行/本列、对齐(左/中/右 → GFM 冒号分隔线,往返验证);Tab 移下一格(Shift+Tab 上一格,goToNextCell);表尾 Enter 加行为 PM 表格默认语义(头行行尾 Enter 也在单元格内加行)。

- 任务列表:复选框点击切换完成态(引擎 DOM 委托:preventDefault 浏览器默认勾选,状态由文档 attrs 驱动重渲;写盘 - [x]/[ ] 往返);缩进 Tab / 反缩进 Shift+Tab,右键同步提供(勾选/缩进/反缩进)。

- 关键实现裁决:PM schema-list 的 sink/lift 与 extension-list 的 taskItem 包装不兼容(实测抛 Invalid content),任务项缩进/反缩进改为「整棵列表节点重组」事务(与解析器产出的嵌套结构同构,taskItem 内嵌 taskList),单步撤销;普通列表仍走 PM sink/lift。taskItem li 渲染无 data-type 属性(实际为 data-checked),选择器与点击委托据此修正。

- 自动化证据:111 单测(勾选/缩进/反缩进/键盘 Tab/表格行列/对齐/撤销重做/往返,真 DOM 点击与键盘事件路径);工作区与单文件竖切回归通过;边界 lint 0。

- 人工复核登记至 20:表格单元格编辑手感、长文滚动性能(首验清单 B-2)、右键菜单真实观感、表尾 Enter 加行真机。
