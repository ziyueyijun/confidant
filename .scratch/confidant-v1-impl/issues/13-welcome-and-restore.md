# 13: 欢迎页与启动恢复

**What to build:** 启动的两条路径(规格第 6 节):**常规启动恢复上次工作区**——直接回到上次打开的文件夹与最后编辑的文件,展开状态还原(04 的树展开记忆同源);**首启/无历史走欢迎页**——产品名 + 一句人话 + 「打开文件夹」主按钮 + 最近打开的文件夹列表(点击即切换,记录数据来自 04)。「文件」菜单「最近打开」同源列表点亮(03 框架置灰位)。

- 恢复/切换工作区:当前已编辑内容经 02 自动保存模型保证不丢(关闭窗口/切换前 flush 由 02 已立,本票只负责「恢复到哪」与展示层)。
- 最近列表为空时欢迎页不显示列表区或显示引导;列表上限与记录格式与 04 约定一致。
- 窗口形态仍为单窗口;欢迎页就是窗口内页面,不是独立窗口。

**Blocked by:** 04(文件树侧栏基座——树展开记忆与最近打开记录数据)。

**Status:** ready-for-agent

- [ ] 有历史时常规启动:直接进入上次工作区,树展开状态还原,最后编辑的文件在编辑区打开(若该文件已不存在,按 12 的处置语义给出提示条,不静默)。
- [ ] 首启/无历史时显示欢迎页:产品名、「打开文件夹」主按钮、最近打开列表(若有记录);点「打开文件夹」进入工作区。
- [ ] 欢迎页列表项点击即切换该工作区;当前会话有未保存输入时切换不丢(02 语义下成立)。
- [ ] 「文件」菜单「最近打开」与欢迎页同一数据源,点击行为一致;条目按最近时间排序,数量有上限。
- [ ] 退出→重开路径反复验证无状态错乱(树展开、当前文件、工作区三者一致还原)。
