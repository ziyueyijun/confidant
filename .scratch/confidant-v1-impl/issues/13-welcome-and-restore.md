# 13: 欢迎页与启动恢复

**What to build:** 启动的两条路径(规格第 6 节):**常规启动恢复上次工作区**——直接回到上次打开的文件夹与最后编辑的文件,展开状态还原(04 的树展开记忆同源);**首启/无历史走欢迎页**——产品名 + 一句人话 + 「打开文件夹」主按钮 + 最近打开的文件夹列表(点击即切换,记录数据来自 04)。「文件」菜单「最近打开」同源列表点亮(03 框架置灰位)。

- 恢复/切换工作区:当前已编辑内容经 02 自动保存模型保证不丢(关闭窗口/切换前 flush 由 02 已立,本票只负责「恢复到哪」与展示层)。
- 最近列表为空时欢迎页不显示列表区或显示引导;列表上限与记录格式与 04 约定一致。
- 窗口形态仍为单窗口;欢迎页就是窗口内页面,不是独立窗口。

**Blocked by:** 04(文件树侧栏基座——树展开记忆与最近打开记录数据)。

**Status:** ready-for-agent

- [x] 有历史时常规启动:直接进入上次工作区,树展开状态还原,最后编辑的文件在编辑区打开(若该文件已不存在,按 12 的处置语义给出提示条,不静默)。
- [x] 首启/无历史时显示欢迎页:产品名、「打开文件夹」主按钮、最近打开列表(若有记录);点「打开文件夹」进入工作区。
- [x] 欢迎页列表项点击即切换该工作区;当前会话有未保存输入时切换不丢(02 语义下成立)。
- [x] 「文件」菜单「最近打开」与欢迎页同一数据源,点击行为一致;条目按最近时间排序,数量有上限。
- [x] 退出→重开路径反复验证无状态错乱(树展开、当前文件、工作区三者一致还原)。

## 实现记录(13)

- 提交:feat(13)(分支 impl/confidant-v1)。

- 启动路径:常规启动读 lastSession(04/主进程 state-store)→ 打开上次工作区并恢复最后编辑文件(自动保存已覆盖无未保存态);文件不存在 → 12 横幅语义(不静默);无历史 → 欢迎页(产品名 + 打开文件夹 + 最近打开列表,数据与「文件→最近打开」菜单同源 recentFolders)。菜单最近打开改为动态子菜单(重建整表,同名列消歧追加父目录);点击行为与欢迎页一致(切工作区前 flush)。恢复兜底:工作区状态未落到预期根且有文档上下文时自愈补开(调试期观测到的启动竞态,守卫生效,记录在案)。

- 调试记录:首轮 restore 失败根因为主进程 workspaceOpen 每次把 lastSession.file 清 null + 早期状态文件被失败运行反复改写;现每次打开文件(noteOpened)都会回写 file,失败运行不再污染(自检隔离 state 目录)。

- 自动化证据:116 单测;双程持久化竖切:seed(开工作区→开 a.md→键入→自动保存→退出)后同一 state 目录常规启动 → 直接回到工作区与 a.md(无欢迎页、侧栏在、键入标记在编辑器与磁盘);无历史启动欢迎页回归;工作区全链回归;边界 lint 0。人工复核:退出→重开反复路径、菜单最近打开点击真机登记 20。
