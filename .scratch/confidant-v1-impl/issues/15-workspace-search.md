# 15: 全工作区搜索

**What to build:** Ctrl+Shift+F(视图菜单「全工作区搜索」位点亮)与搜索面板作用域切换(规格第 11 节):「当前文件 / 整个工作区」——整个工作区 = 全部 .md 文件子串匹配,结果列表显示**文件与命中的行摘要**,点击结果 → 打开该文件(不在当前视图则切换,自动保存保证不丢)并滚动高亮命中处;实时性:与文件监听(04)同源,反映磁盘现状,不依赖索引——搜索期间外部新增/删除/改名文件即时反映。占位文案:「搜索笔记…」。

- 面板/高亮机制复用 14(同一面板、同一高亮实现,作用域切换);「查找 Ctrl+F」与「全工作区搜索 Ctrl+Shift+F」共用面板,默认作用域区分。
- 子串匹配、无正则/选项面板;结果行摘要展示命中行文本(截断得体)。
- 只搜 .md(与树同口径,隐藏文件不搜)。

**Blocked by:** 14(查找面板与高亮机制——复用同一实现)。

**Status:** ready-for-agent

- [ ] 面板内作用域切换「当前文件/整个工作区」,当前文件模式行为与 14 一致;Ctrl+Shift+F 直达全工作区模式。
- [ ] 全工作区:遍历工作区全部 .md(含子文件夹),命中行按文件分组/列出摘要;点击结果 → 切换打开该文件并滚动高亮命中处;当前文件已自动保存(内容不丢)。
- [ ] 实时性:搜索过程中外部新增/删除/改名 .md,结果即时更新(与监听同源,不建索引);中文与标点子串命中正确。
- [ ] 无结果空态明确;占位文案「搜索笔记…」生效;结果点击后高亮不落盘(保存→重开无残留)。
- [ ] 大工作区(几十至几百文件)搜索可用,不冻结界面(可滚动结果、输入实时)。
