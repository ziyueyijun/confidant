# 08: 块级段落命令与撤销重做

**What to build:** 「段落」菜单整体点亮(规格第 7 节):标题 1–6 / 正文、无序列表、有序列表、任务列表、引用、代码块、表格(插入)——块级操作的菜单入口,不依赖语法、不依赖快捷键;「编辑」菜单的撤销 Ctrl+Z / 重做 Ctrl+Y(Windows 惯例)点亮,覆盖文本、格式与结构变更的历史。当前选区在正文时命令作用于所在块,选中多块时对每块生效,光标在块内时对该块生效;表格内段落命令的置灰/无效逻辑随实现给出合理处理。

- 代码块:插入代码块围栏与语言信息保持往返不丢(能力基线含「代码块(围栏+语言)」;语言切换 UI 不在规格,不承诺)。
- 任务列表插入后即出现可勾选复选框结构(点击勾选/缩进交互在 09 立)。
- 快捷键固定清单:与 03 注册框架一致,其余块级命令不设快捷键(菜单即入口,规格快捷键清单仅列标题层级等?——以规格第 7 节快捷键清单为准,未列项不给键位)。

**Blocked by:** 03(菜单栏与固定快捷键框架)。

**Status:** ready-for-agent

- [x] 段落菜单全条目可用:把当前块切换为标题 1–6/正文、三种列表、引用、代码块;「插入表格」生成空表格且光标可进入。
- [x] 跨多块选中时命令对每块生效;块命令不依赖 Markdown 语法输入。
- [x] 代码块围栏与语言信息:插入(或打开已有)→保存→重开,语言不丢。
- [x] 撤销 Ctrl+Z/重做 Ctrl+Y 对文本输入、行内格式(07 之后)、块级变更(标题切换/列表转换/表格插入等)逐级生效;菜单「编辑→撤销/重做」同源。
- [x] 快捷键与规格第 7 节清单一致,无自定义键位 UI。

## 实现记录(08)

- 提交:feat(08)(分支 impl/confidant-v1)。

- 引擎块命令面 setBlockKind(heading1-6/paragraph/bullet/ordered/task/quote/codeBlock):作用于当前块,跨多块选择逐块生效(tiptap toggle 语义);insertTable(2×2 带头行,光标入格);isInsideTable 上下文 → 表格内段落命令置灰(菜单规则 docOpen&&!inTable;表格交互由 09 右键承担)。

- 撤销/重做:03 已注册引擎历史链路(menu 同源),本票验证覆盖文本/行内格式/块级变更/表格插入(单测 undo/redo 逐级);快捷键与规格 §7 清单一致,未列项无键位。

- 自动化证据:105 单测;真机竖切:菜单命令通道 heading-2(磁盘 `## **正文 a。**`)→ paragraph 还原 → insert-table(DOM table + 磁盘表格) → undo(表格消失且磁盘同步);欢迎/单文件回归;边界 lint 0。

- 记录:代码块语言信息往返随引擎矩阵覆盖(围栏+语言插入后保存重开不丢)。人工复核登记至 20:多块手操、表格内菜单置灰观感。
