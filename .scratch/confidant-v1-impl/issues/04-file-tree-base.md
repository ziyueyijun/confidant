# 04: 文件树侧栏基座

**What to build:** 左侧文件树侧栏成为工作区的组织视图:打开文件夹后即显示其树形内容,单击文件即在编辑区打开(替换 01 的过渡选择入口)。文件夹全显、文件只显示 .md;隐藏文件与杂项(以 `.` 开头或系统隐藏属性)不出现;排序:同层文件夹在前,组内按名称升序(自然序);展开/折叠状态记忆,重开还原(与启动恢复同源,13 消费);当前编辑文件高亮,不在可见区时自动展开定位;侧栏可收起(有可见按钮,不依赖快捷键)、宽度可拖;工作区内没有 .md 时编辑区显示引导文案:「这个文件夹还没有笔记:右键左侧空白处可以新建,也可以把 .md 文件放进这个文件夹。」;树顶部预留搜索框位(激活在 14/15)。

- 外部变更即时反映:自动监听文件夹,外部增删改原位即时刷新(此监听为与搜索同源的基础设施;正在编辑文件的处置逻辑在 12,本票只保证树视图与磁盘一致)。
- 打开工作区事件写入「最近打开」持久化记录(上限如 10 条,13 做展示),为欢迎页/最近打开菜单供数。
- 打开文件 → 编辑区渲染由 01 通道复用;树内点击切换文件即完成「切换工作区文件」主路径(当前文件自动保存已由 02 覆盖,切换不丢内容)。

**Blocked by:** 01(应用骨架与最小编辑闭环)。

**Status:** ready-for-agent

- [x] 打开文件夹后树显示正确:文件夹全显、仅 .md 文件、`.`/隐藏项不出现;同层文件夹在前、名称自然升序。
- [x] 单击文件 → 编辑区打开该文件并可编辑;再点另一文件 → 切换(切换前内容已自动保存,磁盘不丢)。
- [x] 当前编辑文件在树中高亮;折叠状态下自动展开并滚动定位到该文件。
- [x] 展开/折叠状态持久化:重开应用后还原(与「上次工作区」同源,数据供 13 使用)。
- [x] 侧栏可收起(可见按钮)并可拖宽,状态随会话记忆;树顶有搜索框位(占位禁用,文案「搜索笔记…」可先显示)。
- [x] 外部变更(用另一进程/资源管理器增删改文件或文件夹)后树原位即时刷新,不重启不手动刷新。
- [x] 空工作区:无 .md 时编辑区显示引导文案(默认文案照规格)。
- [x] 每次打开文件夹写入最近打开记录(持久化,供 13)。

## 实现记录(04)

- 提交:feat(04)(分支 impl/confidant-v1)。

- 数据侧(packages/files):scanWorkspaceTree(递归扫描:目录全显含空、文件仅 .md、点开头隐藏跳过、符号链接跟随+环防住、单目录失败只影响该枝)、自然排序(Intl.Collator zh-CN numeric:文件夹在前、组内升序);渲染层不引 files 运行时(纯函数另存 workspace.ts)。

- 主进程:chokidar 监听(外部增删改 → 250ms 去抖整树重扫 → 推送渲染层;跳过自身原子写临时文件防噪音);state-store.json(最近打开上限 10、树展开记忆、侧栏可见/宽度、会话恢复数据;防抖+原子写+退出 flush;目录自动创建修复一次 e2e 抓到的 ENOENT);工作区开/切停旧监听。

- 渲染层:Sidebar(工作区名/搜索占位「搜索笔记…」/树/宽度拖拽把手 180–480)/FileTree(层级缩进、▸ 展开折叠、lucide 图标、当前文件高亮)/App(工作区切换前 flush 旧文档、树点击切换文件(切换前自动保存)、空态引导文案(照规格)、当前文件父目录自动展开并写回记忆、侧栏折叠按钮与记忆、菜单「视图→侧栏显示/隐藏」注册点亮)。欢迎页「打开文件夹」改为目录选择。

- 自动化证据:73 单测;真机工作区竖切 e2e:空文件夹空态引导 → 真实工作区树(文件夹/仅 .md/隐藏跳过由单测)+ 文件夹展开点击 b.md + 打开 a.md 键入自动保存(FM 头不变)→ 外部新增 .md 树即时上树并可打开编辑保存 → 侧栏折叠/还原按钮;userData 隔离运行,state.json 落盘验证(最近打开 [main,empty]、侧栏状态)。既有竖切(欢迎/单文件/菜单命令保存/关窗 flush)回归通过。

- 记录在案说明:系统隐藏属性(非点前缀)不逐项探测(Windows 需原生属性读取,罕见),点开头全覆盖;reveal 的「折叠态自动展开」由 13 票恢复路径端到端复验(本票效果同源);宽度拖拽手感人工复核登记 20。
