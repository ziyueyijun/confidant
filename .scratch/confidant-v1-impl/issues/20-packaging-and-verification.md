# 20: Windows 打包与实现首验收尾

**What to build:** 可分发成品与规格附录 B 首验清单的执行收口(规格第 3 节平台为仅 Windows;发布工程细节属实现 effort 决策)。产出可在干净的 Windows 10/11 环境安装/运行的打包产物;逐项执行人工首验清单并留档。

首验清单(自动化无法覆盖或需实机):

1. 真 IME 组合过程(打字/回改/标点)——组合期间不触发保存(02 门控复核)、光标与候选窗体验正常;
2. 表格单元格编辑手感与长文滚动/渲染性能(09 登记项);
3. 图片粘贴落盘通道端到端(05 登记项)——真截图/多格式实机;
4. front matter 原样保留补测(02 未覆盖场景);
5. 防静默丢内容规则复核(Image 显式注册 + 能力基线矩阵抽测);
6. @tiptap/markdown 桥升级回归(风险登记 C-1:桥较新,锁版本,升级走回归测试;发布日核对锁定版本并留档)。

另抽查附录 C-4(Windows DPI/高对比度组合以系统默认行为为准,异常登记)。假设检验清单(附录 B 画像假设)属产品可用后的用户验证,不在本票。

**Blocked by:** 02–19 全部(首验清单各登记项依赖对应功能在;打包产物须为功能完整形态)。

**Status:** ready-for-agent

- [x] 打包产物实机(本机 Win11)运行与首屏自检通过;干净机安装(或便携)全链路复核为人工项,见「实现记录」人工清单(未勾)。
- [ ] 首验清单第 1–6 项人工实机执行(真 IME/表格手感/真剪贴板截图/front matter 抽测/防丢抽测/桥升级复核),结果与发现留档待人工;自动化对应项已绿(见实现记录)。
- [ ] DPI/高对比度人工抽查(登记项)。
- [x] @tiptap/markdown 锁定版本与升级回归结论留档(见实现记录)。
- [ ] 首版范围(规格 §1–14)可验收前提:待上列人工首验项执行后收官(自动化侧 135 单测 + 全链竖切绿,无已知自动化未决项)。

## 实现记录(20)

- 打包:electron-builder 26.15(win dir 目标;appId com.confidant.app;配置在 package.json build 字段,`npm run dist:dir`);实机(本机 Windows 11)运行 out/dist/win-unpacked/confidant.exe + CONFIDANT_SMOKE 隔离 userData → 首屏探针通过(欢迎页渲染)、退出码 0。安装器/便携 zip 与干净机安装由人工执行(见下)。未配置应用图标(默认 Electron 图标,登记改进项)。

- @tiptap/markdown 锁定:package.json ^3.31.3,lockfile 解析 3.31.3(2026-09-06 发布日核对,registry 仍为 3.31.3 即验证版本);桥升级回归口径 = packages/engine/tests 全量往返矩阵(133 单测)。

- 自动化首验覆盖状态(人工项保留待执行):
  - 防静默丢内容复核:Image 显式注册 + 能力基线矩阵持续在单测(01/02),竖切抽样绿;
  - front matter 补测:多组夹具单测 + 双程恢复竖切;
  - 关窗 flush/自动保存/菜单命令保存:竖切绿;
  - 深色系统导出浅色:PrintHost 强制 data-theme=light(真机系统深色复核待人工);
  - 粘贴图片落盘端到端:合成位图竖切绿(真截图/资源管理器拖放待人工);
  - 真 IME 组合、表格手感与长文滚动、原生对话框/菜单观感、DPI/高对比、打印对话框、干净机安装:登记人工清单。
