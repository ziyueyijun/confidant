/**
 * 原型的假数据。全部在内存里，不落盘。
 * 内容刻意混入中英混排，长句，各种标点，好让「中文排版手感」暴露出来。
 *
 * 文件树支持任意层级嵌套（文件夹套文件夹套文件……）。
 */

export interface Note {
  path: string
  title: string
  body: string
}

// 递归树节点：文件夹 or 文件
export type FileTreeNode =
  | { kind: 'file'; path: string; title: string }
  | { kind: 'folder'; name: string; children: FileTreeNode[] }

const 长句 = `我记得那天下午读到一个说法：**知识的价值不在于收集，而在于连接**。这个观点本身并不新鲜——Zettelkasten 的方法论讲了三十年——但它在中文语境里有个特别的困难：中文没有天然的单词边界，所以"连接"这件事在中文笔记里比在英文里更难用搜索发现。这正是我为什么要自己写这个软件。`

const 技术笔记 = `## 为什么是逐字索引

实测数据摆在这里：

| 查询类型 | 预分词召回 | 逐字召回 |
| --- | --- | --- |
| 单字 | 27% | 100% |
| 跨词子串 | 31% | 100% |

**关键发现**：\`wangfenjin/simple\` 那个看似复杂的 C++ 扩展，本质就是逐字建 token，不是词级分词。

> 所以能用一行纯 JS 正则复刻——这是整件事最反直觉的地方。

代价是索引膨胀 5.18 倍，但 *100% 召回* 值这个价。

### 边界

逐字索引解决不了语义近似——搜"电脑"找不到只写了"计算机"的那条。这是词表方案的长处，也是它的代价：词表要么人工维护，要么用模型猜，两者都不透明。

我的判断是先不做。检索的第一优先级是**不漏**，第二才是**不烦**。漏了用户不知道，烦了用户知道该往哪调。`

const 正则备忘 = `## 常用片段

提取 markdown 链接的标题：

\`\`\`
\\[([^\\]]+)\\]\\(([^)]+)\\)
\`\`\`

中文标点前后加空格（避免挤压）：

\`\`\`
([\\u4e00-\\u9fa5])([a-zA-Z0-9])
\`\`\`

### 注意

\`\\u4e00-\\u9fa5\` 只覆盖基本汉字区，扩展 A/B 区的字（生僻字、异体字）不在内。真要做全，得用 \`\\p{Script=Han}\`，但那需要 \`u\` 标志，且 JS 引擎支持情况不一。`

const 工具箱 = `随手记的命令和配置。

- \`git log --oneline --graph --all\` —— 看全貌
- \`rg --files-with-matches\` —— 只要文件名，别刷屏
- \`du -sh */ | sort -h\` —— 谁占地方

**待补**：备份脚本还没写。`

const 架构草案 = `## 分层

主进程管文件与索引，渲染进程只管画。边界划在 IPC 上——

- 主进程：文件读写、监听、索引、WebDAV
- 渲染进程：编辑器、界面
- IPC：**只传数据，不传行为**

### 为什么不让渲染进程直接碰文件

因为它随时可能崩，而且它跑的是第三方代码（编辑器扩展、未来的插件）。文件句柄不该出现在那里。`

const 索引实测 = `今天把逐字索引的实测数据补完了。

结论比预想的干净：预分词方案在单字查询上只有 27% 召回，跨词子串 31%——两个数字都低得没法用。而逐字方案都是 100%。

代价是索引膨胀 5.18 倍。本地软件，这点空间换召回率，划算。`

/** 语法全覆盖——每一种 Markdown 语法都出现一次，用来看渲染对不对 */
const 语法全集 = `# 一级标题

## 二级标题

### 三级标题

#### 四级标题

##### 五级标题

###### 六级标题

这是一段普通正文，包含**粗体**、*斜体*、***粗斜体***、~~删除线~~、\`行内代码\`，
以及一个[链接](https://example.com)和一个裸地址 https://example.org 。

中文与 English 混排、标点：全角，半角, 括号（圆）【方】《书名》。

> 这是一段引用。
> 引用可以有多行。
>
> 引用里还能有**粗体**。

---

- 无序列表第一项
- 第二项
  - 嵌套一层
    - 再嵌一层

1. 有序列表第一项
2. 第二项
3. 第三项

- [ ] 未完成的任务
- [x] 已完成的任务

\`\`\`javascript
function greet(name) {
  const message = "Hello, " + name + "!"
  return message
}

console.log(greet("World"))
\`\`\`

\`\`\`
这是代码块。
  缩进会保留。
const x = 1
\`\`\`

| 语法 | 支持 | 备注 |
| --- | --- | --- |
| 标题 | ✅ | H1–H6 |
| 表格 | ✅ | 可直接编辑 |
| 代码块 | ✅ | 围栏式 |

![图片示例](assets/001/示例.png)

最后一段：行尾两个空格换行
这一行应当紧接在上一行下面（软换行）。

段落之间空一行会分开。`

export const NOTES: Note[] = [
  { path: '语法全集.md', title: '语法全集', body: 语法全集 },
  { path: '技术/为什么是逐字索引.md', title: '为什么是逐字索引', body: 技术笔记 },
  { path: '思考/知识与连接.md', title: '知识与连接', body: 长句 },
  { path: '技术/待整理/正则备忘.md', title: '正则备忘', body: 正则备忘 },
  { path: '技术/待整理/工具箱.md', title: '工具箱', body: 工具箱 },
  { path: '思考/长期项目/第二大脑/架构草案.md', title: '架构草案', body: 架构草案 },
  { path: '技术/索引体积实测.md', title: '索引体积实测', body: 索引实测 },
]

// 任意层级嵌套的文件树示例
export const 文件树: FileTreeNode[] = [
  { kind: 'file', path: '语法全集.md', title: '语法全集' },
  {
    kind: 'folder', name: '技术',
    children: [
      { kind: 'file', path: '技术/为什么是逐字索引.md', title: '为什么是逐字索引' },
      { kind: 'file', path: '技术/索引体积实测.md', title: '索引体积实测' },
      { kind: 'folder', name: '待整理', children: [
        { kind: 'file', path: '技术/待整理/正则备忘.md', title: '正则备忘' },
        { kind: 'file', path: '技术/待整理/工具箱.md', title: '工具箱' },
      ]},
    ],
  },
  {
    kind: 'folder', name: '思考',
    children: [
      { kind: 'file', path: '思考/知识与连接.md', title: '知识与连接' },
      { kind: 'folder', name: '长期项目', children: [
        { kind: 'folder', name: '第二大脑', children: [
          { kind: 'file', path: '思考/长期项目/第二大脑/架构草案.md', title: '架构草案' },
        ]},
      ]},
    ],
  },
]
