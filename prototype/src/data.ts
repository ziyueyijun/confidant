/**
 * 原型的假数据。全部在内存里，不落盘。
 * 内容刻意混入中英混排，长句，各种标点，好让「中文排版手感」暴露出来。
 *
 * 文件树支持任意层级嵌套（文件夹套文件夹套文件……）。
 */

export interface Note {
  path: string
  title: string
  tags: string[]
  created: string
  body: string
}

// 递归树节点：文件夹 or 文件
export type FileTreeNode =
  | { kind: 'file'; path: string; title: string }
  | { kind: 'folder'; name: string; children: FileTreeNode[] }

const 长句 = `我记得那天下午读到一个说法：**知识的价值不在于收集，而在于连接**。这个观点本身并不新鲜——Zettelkasten 的方法论讲了三十年——但它在中文语境里有个特别的困难：中文没有天然的单词边界，所以"连接"这件事在中文笔记里比在英文里更难用搜索发现。这正是我为什么要自己写这个软件。`

const 技术笔记 = `## 为什么是逐字索引

实测数据摆在这里：

| 查询类型 | 预分词召回 | 逐字召回 |
| --- | --- | --- |
| 单字 | 27% | 100% |
| 跨词子串 | 31% | 100% |

**关键发现**：\`wangfenjin/simple\` 那个看似复杂的 C++ 扩展，本质就是逐字建 token，不是词级分词。

> 所以能用一行纯 JS 正则复刻——这是整件事最反直觉的地方。

代价是索引膨胀 5.18 倍，但 *100% 召回* 值这个价。

### 边界

逐字索引解决不了语义近似——搜"电脑"找不到只写了"计算机"的那条。这是词表方案的长处，也是它的代价：词表要么人工维护，要么用模型猜，两者都不透明。

我的判断是先不做。检索的第一优先级是**不漏**，第二才是**不烦**。漏了用户不知道，烦了用户知道该往哪调。`

const 日记 = `今天想到两件事。

第一件是关于[信息架构](#)的：\`收件箱\` 应该是一个真实文件，不是一个虚拟队列。因为虚拟队列意味着"东西不在文件系统里"，那正是我从思源逃出来的原因。

第二件更私人一些——我发现自己记笔记的频率和心情好坏强相关。心情好的时候一天记十几条，差的时候一整周都不打开。这或许说明这个软件的第一优先级应该真的是**捕捉**，而不是整理。

- [ ] 验证捕捉窗口的手感
- [x] 把逐字索引的方案定下来
- [ ] 想清楚日记的入口放哪`

const 正则备忘 = `## 常用片段

提取 markdown 链接的标题：

\`\`\`
\\[([^\\]]+)\\]\\(([^)]+)\\)
\`\`\`

中文标点前后加空格（避免挤压）：

\`\`\`
([\\u4e00-\\u9fa5])([a-zA-Z0-9])
\`\`\`

### 注意

\`\\u4e00-\\u9fa5\` 只覆盖基本汉字区，扩展 A/B 区的字（生僻字、异体字）不在内。真要做全，得用 \`\\p{Script=Han}\`，但那需要 \`u\` 标志，且 JS 引擎支持情况不一。`

const 工具箱 = `随手记的命令和配置。

- \`git log --oneline --graph --all\` —— 看全貌
- \`rg --files-with-matches\` —— 只要文件名，别刷屏
- \`du -sh */ | sort -h\` —— 谁占地方

**待补**：备份脚本还没写。`

const 架构草案 = `## 分层

主进程管文件与索引，渲染进程只管画。边界划在 IPC 上——

- 主进程：文件读写、监听、索引、WebDAV
- 渲染进程：编辑器、界面
- IPC：**只传数据，不传行为**

### 为什么不让渲染进程直接碰文件

因为它随时可能崩，而且它跑的是第三方代码（编辑器扩展、未来的插件）。文件句柄不该出现在那里。`

const 九月二十日 = `今天把逐字索引的实测数据补完了。

结论比预想的干净：预分词方案在单字查询上只有 27% 召回，跨词子串 31%——两个数字都低得没法用。而逐字方案都是 100%。

代价是索引膨胀 5.18 倍。本地软件，这点空间换召回率，划算。`

export const NOTES: Note[] = [
  { path: '技术/为什么是逐字索引.md', title: '为什么是逐字索引', tags: ['技术', '检索'], created: '2026-09-20', body: 技术笔记 },
  { path: '思考/知识与连接.md', title: '知识与连接', tags: ['认知'], created: '2026-09-18', body: 长句 },
  { path: 'journal/2026/2026-09-25.md', title: '2026-09-25', tags: ['日记'], created: '2026-09-25', body: 日记 },
  { path: '技术/待整理/正则备忘.md', title: '正则备忘', tags: ['技术'], created: '2026-09-12', body: 正则备忘 },
  { path: '技术/待整理/工具箱.md', title: '工具箱', tags: ['技术'], created: '2026-09-10', body: 工具箱 },
  { path: '思考/长期项目/第二大脑/架构草案.md', title: '架构草案', tags: ['架构'], created: '2026-09-05', body: 架构草案 },
  { path: 'journal/2026/2026-09-20.md', title: '2026-09-20', tags: ['日记'], created: '2026-09-20', body: 九月二十日 },
]

// 任意层级嵌套的文件树示例
export const 文件树: FileTreeNode[] = [
  {
    kind: 'folder', name: '技术',
    children: [
      { kind: 'file', path: '技术/为什么是逐字索引.md', title: '为什么是逐字索引' },
      { kind: 'folder', name: '待整理', children: [
        { kind: 'file', path: '技术/待整理/正则备忘.md', title: '正则备忘' },
        { kind: 'file', path: '技术/待整理/工具箱.md', title: '工具箱' },
      ]},
    ],
  },
  {
    kind: 'folder', name: '思考',
    children: [
      { kind: 'file', path: '思考/知识与连接.md', title: '知识与连接' },
      { kind: 'folder', name: '长期项目', children: [
        { kind: 'folder', name: '第二大脑', children: [
          { kind: 'file', path: '思考/长期项目/第二大脑/架构草案.md', title: '架构草案' },
        ]},
      ]},
    ],
  },
  {
    kind: 'folder', name: 'journal',
    children: [
      { kind: 'folder', name: '2026', children: [
        { kind: 'file', path: 'journal/2026/2026-09-25.md', title: '2026-09-25' },
        { kind: 'file', path: 'journal/2026/2026-09-20.md', title: '2026-09-20' },
      ]},
    ],
  },
]

export const 捕捉示例 = [
  '突然想到：搜索框应该支持拼音，但只搜标题',
  '读《卡片笔记写作法》第 3 章 —— 关于"永久笔记"的定义值得抄下来',
  '明天记得试试逐字索引在 10 万篇笔记上的表现',
]
