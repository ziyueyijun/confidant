# 知己笔记

本地优先的个人知识管理桌面应用。笔记是用户自己的纯 Markdown 文件；应用提供编辑、检索与同步，但不拥有数据——任何编辑器都可以改这些文件，应用必须照常工作。

## 库与文件

**库**：
用户笔记的根文件夹，可被应用打开与切换。库是自包含的——配置、回收站、冲突副本都在库内。
_Avoid_: vault、graph、工作空间、知识库

**笔记**：
库中的一条记录，物理上是一个 Markdown 文件。笔记的标题即其文件名（经规范化）。
_Avoid_: 文档、页面、卡片、条目

**附件**：
库中非笔记的文件——图片、PDF、音频等。附件与笔记一同同步，但不参与检索。
_Avoid_: 资源、素材、asset

**配置目录**：
库根下的 `.confidant/`，存放应用自己的簿记（配置、回收站、冲突副本）。**它是库的一部分，但不是库本身**——它可以被删除，删掉只丢配置，不丢笔记。
_Avoid_: 隐藏目录、系统目录

**回收站**：
应用内删除的笔记的暂存处（`.confidant/trash/`），可还原。**外部删除不进回收站**——在文件管理器或其他程序里的删除由操作系统负责，应用不假装能撤销它。

## 组织与记录

**元数据区**：
笔记顶部以 `---` 围起的结构化信息区，用于声明别名、置顶等。**未知字段一律原样保留**。正文以 `---` 开头但内容不是合法结构化信息时，整段视为正文。
_Avoid_: frontmatter、YAML 头、属性区

**别名**：
一篇笔记的另一个名字，声明在元数据区。它让同一篇笔记能被多个名字链接与搜到。
_Avoid_: 同义词、title

## 链接

**小节**：
笔记中由一个标题及其下内容构成的片段。**小节是链接的最小粒度**——段落不可被单独链接。
_Avoid_: 块、段落、章节

**小节链接**：
指向另一篇笔记之小节的引用，写法为 `[[文件名#小节]]`，可附加显示文本。链接只表达引用关系，不复制内容。
_Avoid_: 块引用、双向链接、wiki 链接

**反向链接**：
指向当前笔记的那些小节链接——「谁引用了我」。反向链接由索引推导，不是被写入文件的。

**断链**：
目标不存在的链接。「不存在」有两种来源：小节标题被改，或目标笔记被删。

## 检索

**索引**：
为检索而建立的笔记内容的派生物。**索引必须能从笔记文件完全重建**——删掉它只损失检索速度（与重建时间），不损失任何内容。

**引用的检索**：
检索时，引用了命中笔记的那些笔记也作为结果出现，但排在直接命中之后并予以标注。

## 同步

**远端**：
存放库的一份副本的外部位置，由用户自备。应用负责把本地的改动送上去、把别处的改动取回来。

**同步锚点**：
本机记录的「上次同步时远端长什么样」。**它可丢弃但不可重建**——删掉它不丢内容，但会丢失归因能力，导致所有文件被判为冲突。锚点属于本机，不随库同步。

**冲突**：
同一篇笔记在两处都被改动，且两处改动无法合并为一个结果的状态。

**冲突副本**：
冲突无法自动合并时保留下来的额外一份笔记（存于 `.confidant/conflicts/`），供用户手工取舍。它是库里的真实文件，不是应用内部的记录。
_Avoid_: 冲突文件、重复文件
