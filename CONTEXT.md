# confidant(知己笔记)

本地优先、所见即所得的单窗口 Markdown 桌面笔记(Electron + React + TipTap)。领域裁决主要沉淀在 `.scratch` 票与首版规格 spec.md;本文件只收录容易出现同义词漂移的术语。

## Language

**欢迎页**:
未打开工作区时的落点页(组件 Welcome.tsx),含产品名与标语行(不再陈列最近打开列表)。用户口语常称「首页」;与「空工作区引导」(已有工作区但无 .md 时的文案)是两回事。
_Avoid_: 首页, 空状态

**标语行**:
欢迎页产品名下方的一句定位语,当前文案「最懂你的笔记软件」。
_Avoid_: 副标题

**最近打开**:
最近打开的工作区列表(state.json 的 recentFolders 键,主进程在打开工作区时记录,上限 10)。UI 展示仅存在于「文件 → 最近打开」菜单,欢迎页不再陈列。
_Avoid_: 最近打开程序, 最近使用

**浮动工具条**:
选中文本时在选区附近浮现的格式按钮组(加粗/斜体/删除线/链接/清除格式,组件 FormatOverlay)。已按 29 裁决移除——选中文本不再浮现任何工具条;07 起格式入口为底部悬浮**格式工具栏**(见下)。
_Avoid_: 浮动工具栏, 悬浮工具条

**格式工具栏**:
底部悬浮胶囊格式按钮组(组件 FormatToolbar.tsx,12 按钮三组 + 标题下拉):默认隐藏,偏好设置「外观 → 显示工具栏」开启且非源码模式时显示;无独立显隐快捷键。命令与「格式/段落」菜单同一引擎命令面。
_Avoid_: 浮动工具条, 工具栏(口语可)

**大纲面板**:
侧边栏「大纲」tab(组件 OutlinePanel.tsx):渲染层解析 doc 树提取 h1–h6,点击跳转/滚动跟随高亮/层级折叠;源码模式下禁用空态。
_Avoid_: 目录, 文档导航

**页脚状态条**:
底部 28px 信息条(组件 Footer.tsx):右常驻字数(口径 = 中文字符 + 英文单词)与光标段落类型、左「专注模式/打字机」按钮(hover 浮现);点击字数弹四口径明细浮层。
_Avoid_: 状态栏, 底部栏

**视图模式**:
专注模式(F8,非焦点块淡出为 --blur-text)与打字机模式(F9,光标行垂直居中)的总称;两者可叠加、与源码模式互斥(进入源码自动退出,退出恢复)。
_Avoid_: 写作模式, 聚焦模式

**链接面板**:
插入/修改链接的独立弹层(链接地址 + 显示文本输入,含「移除链接」)。触发:「格式 → 链接」Ctrl+K;光标在链接内时呼出进入编辑态。原为浮动工具条内组件,29 起独立。
_Avoid_: 链接对话框

**源码模式**:
与所见即所得相对的全文件原始 Markdown 编辑视图(快捷键 Ctrl+/):文本区内容即磁盘字节(含 front matter),不经引擎序列化;段落/格式/查找等 WYSIWYG 专属命令不可用。曾为规格明确排除项,后新增。
_Avoid_: 源代码模式, 源模式, raw 模式

**侧栏**:
左侧面板(组件 Sidebar.tsx),顶部「文件 / 大纲」双 tab;显示/隐藏(菜单「视图 → 侧栏显示/隐藏」,Ctrl+Shift+L)与宽度经 state.json 记忆;未打开工作区时显示空态框体,开关仍可操作。
_Avoid_: 文件树面板, 侧边栏(口语可)

**偏好设置窗口**:
独立窗口(菜单「文件 → 偏好设置…」,Ctrl+,):左侧分类列表(外观/编辑器,预留扩展位)、右侧设置区。外观含「显示工具栏」开关(默认关);编辑器含代码块自动换行/显示行号开关(自原「设置」菜单迁入,默认均开)。改动经 state:changed 广播跨窗同步。
_Avoid_: 设置菜单, 选项, 首选项

**主题**:
顶级菜单「主题」三勾选(GitHub/Night/Newsprint,data-theme 三值整体切换);「跟随系统」已取消。主题即亮暗选择。
_Avoid_: 外观, 浅色/深色(旧「视图 → 外观」子菜单已删除)

**窗口状态**:
持久化的窗口几何记忆(state.json 的 window 键):是否最大化 + 正常态 bounds;启动恢复,屏外回退默认;最小化不作为记忆。
_Avoid_: 窗口大小记忆, 最大化记忆

**工作区**:
用户打开的一个本地文件夹,应用扫描并读写其中的 .md 文件(代码与规格中的 workspace)。
_Avoid_: 文件夹, 目录(口语混用;代码与规格以「工作区」为准)
